## هدف
پهن‌تر کردن ستون/پنل «Library» در داشبورد.

## تغییر
- فایل: `src/modules/generator-ui/pages/DashboardPage.tsx` (خط ۶۵۵۱)
- کلاس‌های عرض فعلی پنل Library:
  `sm:w-80 lg:w-80 xl:w-96 2xl:w-[26rem]`
- تغییر به مقادیر بزرگ‌تر، مثلاً:
  `sm:w-96 lg:w-[26rem] xl:w-[30rem] 2xl:w-[34rem]`
- مقدار حالت موبایل (`w-[min(22rem,calc(100vw-1.5rem))]`) بدون تغییر می‌ماند تا در صفحه‌های کوچک از کادر بیرون نزند.

## ملاحظات
- فقط UI سمت کلاینت؛ بدون تغییر منطق یا بک‌اند.
- محتوای داخل پنل (Final Videos / Drafts) خودکار با عرض جدید هماهنگ می‌شود.