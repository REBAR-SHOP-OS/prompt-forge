## تغییر
اضافه کردن گزینه **15s** در کنار 5s و 10s در باکس انتخاب طول کلیپ.

### فایل: `src/modules/generator-ui/pages/DashboardPage.tsx`

1) خط 297 — افزودن 15 به نوع state:
```ts
const [durationSeconds, setDurationSeconds] = useState<5 | 10 | 15>(5)
```

2) خط 2372 — افزودن 15 به آرایه گزینه‌ها:
```ts
{([5, 10, 15] as const).map((sec) => {
```

## چرا امن است
- مقدار پیش‌فرض 5 ثابت می‌ماند.
- `durationSeconds` در همان شکل (number) به API ارسال می‌شود (خطوط 1073/1083/1094/1104) و تغییر مدل نیست.
- اگر provider مقدار 15 را نپذیرد، API خطا برمی‌گرداند و توست خطای موجود نمایش داده می‌شود — بدون شکستن UI.