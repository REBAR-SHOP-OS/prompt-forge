هدف: در پاپ‌اوور تنظیمات وویس‌اور (آیکون اسلایدر کنار دکمه‌ی Voiceover)، بخش «Original clip audio» حذف شود و محتویات «Set timing on video…» به‌صورت کامل و درجا داخل همان پاپ‌اوور نمایش داده شود؛ دکمه/دیالوگ جداگانه‌ی «Set timing on video…» حذف شود.

تغییرات (همگی در `src/modules/generator-ui/pages/DashboardPage.tsx`):

1. حذف بخش «Original clip audio»
   - کل اسلایدر «Original clip audio» و متن توضیحی «Clip audio is controlled from the Soundtrack dialog…» از داخل پاپ‌اوور حذف می‌شود.

2. درون‌خطی‌کردن تنظیم زمان
   - دکمه‌ی «Set timing on video…» حذف می‌شود.
   - محتویات فعلیِ دیالوگ زمان‌بندی (موج صدا `SoundtrackWaveform` برای انتخاب بخش صدا + اسلایدرهای Start/End روی تایم‌لاین ویدئو + متن راهنما) مستقیماً زیر اسلایدر «Voiceover» در همان پاپ‌اوور قرار می‌گیرد.
   - عرض پاپ‌اوور کمی بیشتر می‌شود تا موج صدا و اسلایدرها جا شوند و قابل استفاده باشند.

3. پاک‌سازی
   - دیالوگ مستقل `isVoiceoverTimingOpen` و استیت مربوط به آن حذف می‌شود (در صورت بی‌استفاده‌ماندن state آن نیز پاک می‌شود).
   - همان رفکای موج صدا (`voiceoverWaveformRef`) و state‌های `voiceoverRange` / `voiceoverTimeline` بدون تغییر منطق استفاده می‌شوند تا رفتار زمان‌بندی دقیقاً مثل قبل (و مثل موزیک) باقی بماند.

نکته‌ها/حفاظت‌ها:
- فقط UI جابه‌جا می‌شود؛ منطق پخش، gating و رندر نهایی دست‌نخورده می‌ماند.
- اسلایدر voiceover volume حفظ می‌شود.
- پس از تغییر، باز/بسته‌شدن پاپ‌اوور و رندر موج صدا بررسی می‌شود تا درست کار کند.